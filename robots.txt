# The same file on every branch.
#
# Which hosts may be indexed is decided by the build, not here: the live site
# (rosestuffs.org) is indexable, and build.js sends X-Robots-Tag: noindex on every
# response from any other branch.
#
# Never hide a page with Disallow. Blocking the crawl stops a search engine ever
# fetching the page, so it never reads the noindex, and the bare URL can still
# surface in a "site:" search. Use a noindex meta or an X-Robots-Tag instead.

User-agent: *
Allow: /

Sitemap: https://rosestuffs.org/sitemap.xml
